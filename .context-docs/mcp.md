# MCP OAuth authorization server (JFR-100 Phase B1)

OAuth 2.1 authorization server backing the Claude.ai custom connector (MCP). Entirely separate from both the internal v1 shared-secret API and the external org-API-key API — this is a third, distinct auth system. The MCP server that consumes these tokens is documented separately below (Phase B2).

- **Models**: `OAuthClient`, `OAuthAuthorizationCode`, `OAuthAccessToken`, `OAuthRefreshToken` in `schema.prisma`. `OAuthClient.id` (a cuid) doubles as the public OAuth `client_id` — no separate field. All codes/tokens are stored as sha256 hashes (`src/lib/oauth/tokens.ts`), never plaintext, same pattern as `ApiKey`.
- **`@modelcontextprotocol/sdk` is a direct dependency**, added for its OAuth zod schemas/types only (`shared/auth.js` — `OAuthClientMetadataSchema`, etc.). Its `server/auth/router|handlers|middleware` are hard-coupled to Express (`OAuthServerProvider.authorize()` takes an Express `Response`) and cannot be used with Next.js Route Handlers — the actual endpoint logic in this repo is hand-written against the OAuth/MCP spec, using the SDK only for request/response shape validation. It was already a transitive dependency of `shadcn` (devDependency) at a slightly older version — no conflict.
- **Endpoints**: `POST /api/oauth/register` (RFC 7591 DCR — Claude.ai calls this automatically), `GET+POST /oauth/authorize` (consent screen, page not API route since it renders HTML — lives under `src/app/(auth)/oauth/authorize/`), `POST /api/oauth/token` (authorization_code + refresh_token grants).
- **PKCE is mandatory, S256 only** — `code_challenge_method` values other than `S256` (including `plain`) are rejected at `/oauth/authorize`. This matches Claude.ai's actual client behavior, not just the spec's recommendation.
- **Consent screen has no org picker** — it grants access to the user's current `session.user.orgId` only. Building an org-switcher is a documented non-goal elsewhere; don't add one here either.
- **Redirect safety**: `client_id`/`redirect_uri` are validated against the DB *before* any redirect happens. If either is invalid, the page renders a static error — it never redirects, to avoid becoming an open redirect. Once `redirect_uri` is confirmed registered, later validation failures (bad `response_type`, missing/weak PKCE) bounce back to it with `?error=...`, per spec.
- **Refresh tokens rotate on every use, and reuse kills the family (SECH-109)** — `POST /api/oauth/token` with `grant_type=refresh_token` revokes both the old refresh token and its paired access token, then issues a new pair. Every token rotated from one authorization grant shares an `OAuthRefreshToken.familyId`. Presenting an *already-rotated* refresh token is treated as a copied credential: it returns `invalid_grant` **and** revokes every token in that family, so the descendants the thief (or the victim) still holds stop working too (RFC 9700 reuse detection). This is deliberately a different code path from losing the concurrent-claim race — a client that races itself reads `revokedAt: null` and just loses the claim, so its pair survives. Do not collapse the two branches.
- **Authorization codes are single-use** — claimed via a conditional `updateMany({ where: { usedAt: null } })` rather than a plain update, so a concurrent replay of the same code can't race past the check.
- **`requireOAuthToken(request)`** in `src/lib/oauth/require-oauth-token.ts` — bearer-token guard, wired into `/api/mcp` (Phase B2). Returns `{ orgId, userId, clientId, scope }` or a 401 `NextResponse` with a `WWW-Authenticate: Bearer resource_metadata="..."` header (RFC 9728).
- **`/.well-known/*` routes require a `next.config.mjs` rewrite** — Next.js App Router's file-based routing silently skips dot-prefixed directories, so `/.well-known/oauth-authorization-server` and `/.well-known/oauth-protected-resource` (RFC 8414 / RFC 9728 metadata) are implemented under `src/app/well-known/*` and rewritten from `/.well-known/:path*` → `/well-known/:path*`.
- **Middleware exemptions**: `/oauth/authorize` and `/.well-known/` are exempted in `src/middleware.ts` from the default auth-redirect, same pattern as the existing `/invite/` exemption. `/oauth/authorize` needs its own exemption (rather than relying on the exemption) because it must preserve the full query string through a login redirect — the middleware's default `callbackUrl` only carries `nextUrl.pathname`, losing `client_id`/`code_challenge`/etc.
- **Scope model**: `src/lib/oauth/scopes.ts` defines `issues:write`, `search:read`, `docs:read`, `docs:write`, `comments:write`, `comments:read` (added JFR-116) — the tool surface named in JFR-100's description, plus the read-side of comments added after the fact. Enforcement happens per-tool in the MCP server (Phase B2), not here.
- **Route folders starting with `_` are invisible to Next.js App Router routing** — an underscore-prefixed directory (e.g. `src/app/api/oauth/_debug-guard/`) is a Next.js "private folder" convention and gets silently 404'd/ignored by the router. Discovered while writing a throwaway test route — use any non-underscore name for ad-hoc debug routes.

## MCP Streamable HTTP server (JFR-100 Phase B2)

The actual MCP server Claude.ai's connector talks to, at `POST/GET/DELETE /api/mcp`. Consumes the Phase B1 OAuth tokens above via `requireOAuthToken`.

- **Transport**: `WebStandardStreamableHTTPServerTransport` from `@modelcontextprotocol/sdk/server/webStandardStreamableHttp.js` — a Fetch-native (Request/Response) transport, distinct from the Express-coupled `StreamableHTTPServerTransport` that Phase B1 ruled out. It drops straight into a Next.js Route Handler since `NextRequest` is a `Request` and `handleRequest()` returns a `Response` directly.
- **Stateless per-request**: `src/app/api/mcp/route.ts` creates a fresh transport + `McpServer` (from `src/lib/mcp/server.ts`) on every request, with no `sessionIdGenerator`. No cross-request session state to keep in sync if Railway ever runs more than one instance — the rate limiters are DB-backed (SECH-107), so they are multi-instance safe too.
- **Tool surface** (`src/lib/mcp/server.ts`): `search_issues` (`search:read`, reuses the JedForge query language parser/validator/executor from `src/lib/query` and the Search page), `create_issue` (`issues:write`), `list_doc_pages` + `read_doc_page` (`docs:read`), `write_doc_page` create-or-update (`docs:write`, same revision-snapshot-and-prune-at-50 behavior as the Docs UI), `update_issue` (`issues:write`), `link_issues` + `unlink_issues` + `set_issue_parent` (`issues:write`, added JFR-126 — see below), `add_comment` (`comments:write`, added JFR-114) and `list_comments` (`comments:read`, added JFR-116 — read-only, oldest-first, gated on `requireIssueMembership` only, no `canEditIssues` check, same as the doc-read tools). Each tool checks its required scope against the access token's `scope` string itself — a missing scope returns an MCP `isError: true` tool result, not an HTTP error, per spec.
- **`link_issues`/`unlink_issues`/`set_issue_parent` (JFR-126)** mirror the UI's `linkIssue`/`unlinkIssue`/`setIssueParent` server actions (`src/app/(dashboard)/projects/[projectKey]/actions.ts`) but resolve both issues via `requireIssueMembership` (org+project-membership scoped to `ctx.orgId`) rather than `requireProjectMember`/`requireProjectRole`, since the MCP server is OAuth-token-scoped, not session-scoped. `IssueLink` and `Issue.parentId` have no cross-project constraint at the DB level, so both tools enforce same-project in code (returns an error, doesn't just silently allow it). `set_issue_parent` ports the self-parent rejection and depth-50 ancestor-chain cycle-detection walk from `setIssueParent` unchanged — this was flagged in the issue as the one part with real correctness risk. `unlink_issues` accepts either a `linkId` or `(sourceIssueKey, targetIssueKey, linkType)`; both entry paths re-resolve the link by id afterward so permission checks and `ActivityLog` writes share one code path. Both link tools log an `ActivityLog` entry with `field: "link"` (not part of the original `linkIssue`/`unlinkIssue` actions, which don't log activity at all — this was a deliberate JFR-126 addition to make AI-driven links show up in the Activity tab like `update_issue`/`add_comment` do). **Deliberately no `delete_issue`/`close_issue` equivalent added here either** — unlinking/re-parenting isn't destructive to either issue, so it was considered in-bounds despite the no-destructive-tools rule above; this distinction is documented in the JFR-126 issue, not an oversight.
- **`update_issue`/`add_comment` mirror the UI's `ActivityLog`/notification side effects, not just the v1 API's field-handling** — `requireIssueMembership` (alongside `requireProjectMembership`/`requireDocContext`) resolves an issue key scoped to `ctx.orgId` via its project, same org-isolation rule. Both tools gate on `canEditIssues(role)` (the same threshold `write_doc_page` uses) and write `ActivityLog` rows + call `notificationService.statusChanged`/`issueAssigned`/`commentAdded` exactly like the `updateIssue`/`addComment` server actions in `src/app/(dashboard)/projects/[projectKey]/actions.ts` — because unlike the v1 API (Claude Code's own internal loop, not user-facing), these edits land in a shared multi-user product and must show up in the Activity tab / trigger notifications like a human edit would. **Deliberately no `delete_issue`/`delete_comment`/`close_issue`** — this MCP server has no destructive or closing capability by design, not by oversight.
- **No `sessionIdGenerator` means no `mcp-session-id` header** — `initialize` isn't actually required before `tools/call`; each request gets its own fresh server/transport (see "Stateless per-request" above), so a single raw `tools/call` POST works standalone.
- **Faster local/production verification shortcut — skip the full OAuth/DCR/PKCE dance**: since tokens are just `sha256(plaintext)` stored in `OAuthAccessToken.hashedToken` (`src/lib/oauth/tokens.ts` — prefix `mcp_at_`, no salt), you can mint a working bearer token by hand: generate a random token string, hash it, `INSERT INTO "OAuthAccessToken"` directly (needs a matching `OAuthClient` row too — any placeholder `clientName`/`id` works, `tokenEndpointAuthMethod: 'none'`), then `curl -X POST .../api/mcp -H "Authorization: Bearer <token>" -H "Accept: application/json, text/event-stream" -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"...", "arguments":{...}}}'`. **This same trick works against the production DB** via the `DATABASE_PUBLIC_URL` proxy (see `project_railway_cli` memory) + `https://www.jedforge.com/api/mcp` — lets you validate a real deployed MCP tool end-to-end (including its DB side effects, checked via the same `psql` connection) without needing anyone's actual browser login session. Clean up afterward: `DELETE FROM "OAuthAccessToken"`/`"OAuthClient"` for the throwaway rows. No local seed user has `VIEWER` role on any project — to test a permission-denied path, temporarily `INSERT` a `VIEWER` `ProjectMember` row for a seeded user and revert it after.
- **Tenancy**: every tool resolves the target project by key scoped to `ctx.orgId` (never by key alone — keys are globally unique, so an unscoped lookup would let one org's token resolve another org's project). Docs tools require the token's `userId` to be an actual `ProjectMember` of that project — deliberately stricter than the session-based `resolveDocCtx` used by the Docs UI, which lets non-members read *public* docspaces; an OAuth grant acting on a specific user's behalf shouldn't inherit that bypass.
- **`zod` is now a direct dependency** (was only transitive at 3.25.76 via another package, matching the SDK's `^3.25 || ^4.0` peer range) — tool `inputSchema`s import it directly.
- **End-to-end local verification recipe**: DCR (`POST /api/oauth/register`) → Playwright login + click "Approve" on `/oauth/authorize` with `page.route()` intercepting the (unreachable) redirect URI to capture the `code` → PKCE code exchange at `/api/oauth/token` → raw `fetch` calls to `/api/mcp` with `Authorization: Bearer <token>`. The MCP response body is SSE-framed (`data: {...}` lines) unless `enableJsonResponse: true` is set on the transport — this repo left it at the SSE default to match real MCP clients; a test script must strip the `data:` prefix before `JSON.parse`.
- **Claude Code's own `mcp__claude_ai_JedForge__*` tools (available directly in Claude Code sessions, not just Claude.ai) go through this exact server** — they're bound to whichever OAuth token the connector holds, which resolves to Jamie's own `userId`/`orgId`, not to Maximus. None of `add_comment`/`create_issue`/`update_issue`'s schemas accept an `authorId` override (unlike the internal v1 REST API), so any comment or issue created through these tools is attributed to Jamie Edmondson, even when the intent was a Claude Code-authored (Maximus) action. Confirmed 2026-07-31 posting a fix-summary comment on JFR-115 — it landed as Jamie, required a follow-up v1 API call to get the Maximus-authored version. Use the v1 REST API directly whenever the author identity matters.


## SH-021 token verification matrix (SECH-109)

Every scenario in the Security Hardening plan's SH-021 list, mapped to the test that holds it.
All rows are green; run `npm run test:integration` plus `npm test` to re-verify.

| SH-021 scenario | Test | File |
| --- | --- | --- |
| `redirect_uri` matches exactly — no prefix, suffix, query, fragment, case, scheme or port tricks | `validateAuthorizeRequest > refuses <9 variants>, and never redirects there` | `src/integration/auth-flows.itest.ts` |
| A tampered consent form cannot redirect to an unregistered URI | `approveAuthorization / denyAuthorization > never redirects to an unregistered redirect_uri, and skipping PKCE is refused` | `src/integration/auth-flows.itest.ts` |
| The token endpoint rejects a `redirect_uri` that differs from the code's | `POST /api/oauth/token — authorization_code > wrong verifier, wrong redirect_uri, another client, or an expired code are all invalid_grant` | `src/integration/auth-flows.itest.ts` |
| PKCE is required, and only `S256` is accepted — `plain` is rejected | `validateAuthorizeRequest > requires PKCE and accepts only S256 — plain is refused` | `src/integration/auth-flows.itest.ts` |
| Authorization codes are single-use | `POST /api/oauth/token — authorization_code > a code is single-use — a replay gets invalid_grant and no new token` | `src/integration/auth-flows.itest.ts` |
| A concurrent replay of one code cannot race past the single-use check | `POST /api/oauth/token — authorization_code > concurrent replays of one code yield at most one token pair` | `src/integration/auth-flows.itest.ts` |
| Authorization codes expire | `POST /api/oauth/token — authorization_code > wrong verifier, wrong redirect_uri, another client, or an expired code are all invalid_grant` | `src/integration/auth-flows.itest.ts` |
| Wrong `client_id` at the token endpoint | `POST /api/oauth/token — authorization_code > an unknown client_id or a confidential client with the wrong secret is 401` | `src/integration/auth-flows.itest.ts` |
| A registered client cannot redeem another client's code | `POST /api/oauth/register (public by spec) > registering a client grants nothing: it cannot redeem another client's code` | `src/integration/auth-flows.itest.ts` |
| Refresh-token reuse detection revokes the token family | `POST /api/oauth/token — refresh_token > replaying an already-rotated refresh token revokes the descendants it minted` | `src/integration/auth-flows.itest.ts` |
| …and normal rotation does **not** trip it (negative control) | `POST /api/oauth/token — refresh_token > a legitimate rotation chain keeps working and stays in one family` | `src/integration/auth-flows.itest.ts` |
| A refresh token cannot be redeemed by another client | `POST /api/oauth/token — refresh_token > a refresh token cannot be redeemed by another client` | `src/integration/auth-flows.itest.ts` |
| Missing scope is refused per tool call | `MCP tools (OAuth bearer) > scope limits are enforced per tool call` | `src/integration/token-auth.itest.ts` |
| The consent picker cannot mint a code for an org the caller isn't in | `approveAuthorization / denyAuthorization > refuses to mint a code for an org the caller does not belong to` | `src/integration/auth-flows.itest.ts` |
| An expired (or revoked, unknown, malformed) access token is refused | `OAuth bearer guard (REST) > rejects missing, malformed, unknown, expired and revoked tokens with a WWW-Authenticate challenge` | `src/integration/token-auth.itest.ts` |
| A revoked org API key stops working | `external API (org-scoped X-Api-Key) > a revoked key stops working immediately` | `src/integration/token-auth.itest.ts` |
| An org API key dies when its owner leaves the org | `SECH-94 … > adminRemoveOrgMember revokes the leaving member's OAuth tokens and API keys for that org only` | `src/integration/token-auth.itest.ts` |
| An incorrect V1 secret has no side effects, and leaks no length oracle | `internal v1 API (shared secret) > a wrong secret writes nothing and answers identically whatever its length` | `src/integration/token-auth.itest.ts` |
| Secret comparisons are constant-time | `constant-time secret comparison > … uses timingSafeEqual` / `… never compares a secret or its digest with ==/===` | `src/__tests__/constant-time-secrets.test.ts` |
| Codes and tokens are persisted only as sha256 hashes | `credential storage > stores only sha256 hashes — no authorization code or token is recoverable from the database` | `src/integration/auth-flows.itest.ts` |
| A confidential client's secret is persisted only as a hash | `credential storage > stores a registered client's secret only as a hash` | `src/integration/auth-flows.itest.ts` |
| An org API key is persisted as a hash plus a non-reusable display prefix | `org API key storage > stores only a hash and a non-reusable display prefix` | `src/integration/token-auth.itest.ts` |
| A rejected secret is never written to the logs | `requireV1ApiKey > never writes the presented key into the auth-failure log` | `src/lib/__tests__/v1-auth.test.ts` |

**Notes for whoever extends this.**
- The storage rows scan the whole row (`to_jsonb(t)::text LIKE '%plaintext%'`) rather than a named column, so a future column that accidentally echoes a credential is caught too. Each one carries a canary assertion that the *hash* is findable the same way — without it, a broken query would make the test vacuously green.
- The `race vs reuse` boundary in the refresh grant is a timing property and is **not** mutation-proof from the HTTP surface: an earlier attempt to assert it through three concurrent refreshes passed even with family revocation wrongly wired into the race path, because the loser can revoke before the winner has inserted its new row. The rotation-chain row above is the deterministic guard that survives mutation testing; the code comment in `src/app/api/oauth/token/route.ts` carries the rest.
